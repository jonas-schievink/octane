/*use std::ops::{Deref, DerefMut};

/// Wraps a type, prevents access to it, and forces it to be moved.
///
/// This is useful to ensure that a value is used on all paths
pub struct Move<T>(T);

impl<T> Move<T> {
    pub fn take(self) -> T { self.0 }
}
*/
